/** @type {import('next').NextConfig} */
const nextConfig = {
  // Твои настройки (если есть)
  images: {
    domains: [], // если используешь внешние изображения
  },
  // другие опции...
}

module.exports = nextConfig